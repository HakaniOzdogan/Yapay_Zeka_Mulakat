# Privacy

Default behavior: raw audio/video is NOT stored permanently. Only derived metrics and transcripts are stored.

Per-session `SettingsJson` can enable raw media storage; this must be explicitly toggled by the user.
